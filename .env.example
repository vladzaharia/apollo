# Apollo/Sunshine Server Configuration
# Copy this file to .env and update with your actual values

# Apollo/Sunshine server endpoint (include https:// and port)
APOLLO_ENDPOINT=https://192.168.1.100:47990/

# Apollo/Sunshine login credentials
# These are the same credentials you use to log into the web UI
APOLLO_USERNAME=your_username
APOLLO_PASSWORD=your_password

# Notes:
# - The endpoint should include the protocol (https://) and port number
# - Remove trailing slash from endpoint if present (it will be added automatically)
# - Username and password are the same as your Apollo/Sunshine web UI credentials
# - Self-signed SSL certificates are automatically handled
